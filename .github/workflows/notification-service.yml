name: notification-service

on:
  push:
    branches: [main]
    paths:
      - 'notification-service/**'
      - 'common-contracts/**'
  pull_request:
    paths:
      - 'notification-service/**'
      - 'common-contracts/**'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io/${{ github.repository_owner }}

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Test
        run: mvn -pl notification-service -am test

      - name: Package
        run: mvn -pl notification-service -am package -DskipTests

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image
        run: |
          docker build -f notification-service/Dockerfile -t "$REGISTRY"/notification-service:${{ github.sha }} .
          docker push "$REGISTRY"/notification-service:${{ github.sha }}

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG_B64 }}" | base64 -d > ~/.kube/config

      - name: Deploy (rolling update)
        run: kubectl set image deployment/notification-service notification-service="$REGISTRY"/notification-service:${{ github.sha }}
