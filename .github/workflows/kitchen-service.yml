name: kitchen-service

on:
  push:
    branches: [main]
    paths:
      - 'kitchen-service/**'
      - 'common-contracts/**'
  pull_request:
    paths:
      - 'kitchen-service/**'
      - 'common-contracts/**'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io/${{ github.repository_owner }}

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Test
        run: mvn -pl kitchen-service -am test

      - name: Package
        run: mvn -pl kitchen-service -am package -DskipTests

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image
        run: |
          docker build -f kitchen-service/Dockerfile -t "$REGISTRY"/kitchen-service:${{ github.sha }} .
          docker push "$REGISTRY"/kitchen-service:${{ github.sha }}

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG_B64 }}" | base64 -d > ~/.kube/config

      - name: Deploy (rolling update)
        run: kubectl set image deployment/kitchen-service kitchen-service="$REGISTRY"/kitchen-service:${{ github.sha }}
